<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:StreamingHub"
    xmlns:pages="clr-namespace:StreamingHub.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="StreamingHub"
    x:Class="StreamingHub.AppShell">

    <FlyoutItem Title="Home">
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <ShellContent 
        Title="Criar Conteudo" 
        Route="CriarConteudo" 
        ContentTemplate="{DataTemplate pages:CriarConteudo}" />

</Shell>
